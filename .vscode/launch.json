{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch index.html",
        "type": "chrome",
        "request": "launch",
        "url": "${workspaceFolder}/index.html",
        "webRoot": "${workspaceFolder}"
      }
    ]
  }
  